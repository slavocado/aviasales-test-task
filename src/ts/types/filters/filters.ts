export type TransfersFilter = {
  transfersCount: number[]
}

// fastFilter resoponsbile for filtering tickets by flight time, ticket cost and optimal tickets
export type FastFilter = {
  filteringType: number
}
